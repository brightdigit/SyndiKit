name: SyndiKit
settings:
  LINT_MODE: ${LINT_MODE}
packages:
  SyndiKit:
    path: .
aggregateTargets:
  Lint:
    buildScripts:
      - path: Scripts/lint.sh
        name: Lint
        basedOnDependencyAnalysis: false
    schemes: {}